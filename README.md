# Incat Case Project

## 🚀 Getting Started

Follow these steps to run the project locally:

### 1. Clone the repository from GitHub

### 2. Navigate to local project folder with terminal

### 3. Run npm install in the terminal to install dependencies
```bash
npm install
```

### 4. Run npm run dev in the terminal to start the project
```bash
npm run dev
```
